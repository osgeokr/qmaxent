mkdocs==1.6.1
mkdocs-material==9.5.49
mkdocs-static-i18n==1.2.3
pymdown-extensions==10.12

# PDF export (used by the docs CI job to build qmaxent-manual-{en,ko}.pdf).
# Only required for the PDF build step — the regular site build does not need it.
mkdocs-with-pdf==0.9.3
